# PROTOCOL.md — 폰 ↔ PC WebSocket 계약

폰과 PC 두 앱이 공유하는 유일한 진실. 메시지 형식을 바꿀 때는 이 파일부터 고친다.

## 연결

- PC가 WebSocket 서버, `ws://<PC-IP>:9999/ws` 고정 포트.
- mDNS로 `_autodeck._tcp.local.` 광고, 폰은 이걸로 자동 검색·연결 (IP 입력 불필요).

## 원칙

- **배치·매핑은 전부 PC 소유.** exe 경로는 PC 안에만 존재하고 절대 폰으로 전송하지 않는다.
- 폰은 `layout`을 받아 그리기만 하는 얇은 클라이언트. 버튼을 누르면 `press`만 보낸다.
- **그리드는 "칸 개수"만 서버가 정하고, 몇 열×몇 행으로 그릴지는 각 클라이언트가 자기 화면에 맞게 알아서 계산한다.** PC는 폰이 지금 세로인지 가로인지 알 방법이 없으므로, 행/열을 프로토콜에 넣지 않는다.

## 그리드 모델

- 고정 16칸. 슬롯 id는 `"s0"`~`"s15"` (flat index).
- **방사형 다이얼**: 안쪽 링 6칸(`s0`~`s5`) + 바깥쪽 링 10칸(`s6`~`s15`), 두 링이 서로 반대 방향으로 관성 회전한다. 위치는 각 클라이언트가 알아서 계산(고정 각도 없음, 사용자가 돌리면 그 상태 유지).
- 슬롯은 비어있을 수 있다(대상 미배치) — 이 경우 `label`/`icon` 없이 `{ "id": "s3" }`만 보낸다.

## 페어링

같은 WiFi에 있는 아무 기기나 조건 없이 연결을 받아주면, 공용/타인 네트워크에서 낯선 기기가 내 PC 프로그램을 마음대로 실행시킬 수 있다. 그래서 연결 직후엔 반드시 `hello` 핸드셰이크로 페어링을 거치고, `layout`을 포함한 그 어떤 메시지도 그 전엔 오가지 않는다.

- 폰은 최초 실행 시 자기만의 `device_id`(UUID)를 한 번 생성해 영구 저장하고, 매 연결마다 그대로 재사용한다.
- PC는 승인된 기기의 `device_id → token`을 앱 데이터 폴더의 `paired_devices.json`에 저장한다(버튼 배치와 별개 파일).
- 처음 보는 `device_id`이거나 `token`이 안 맞으면, PC는 자기 창을 앞으로 띄우고 사용자에게 허용/거부를 물은 뒤 응답한다(최대 60초 대기, 그 안에 응답 없으면 타임아웃으로 거부 처리).
- 이미 알고 있는 `device_id` + 맞는 `token`이면 대기 없이 즉시 승인.

## 메시지

### 폰 → PC: `hello` (연결 직후 1회, 필수 — 이걸 보내기 전엔 PC가 아무 것도 응답하지 않는다)

```json
{ "type": "hello", "device_id": "3f9c...-uuid", "device": "Galaxy A36", "token": "이전에 발급받은 토큰, 없으면 null" }
```

### PC → 폰: `hello-ack` (페어링 완료 — 이 응답 직후 `layout`이 뒤따라온다)

```json
{ "type": "hello-ack", "token": "이후 재연결에 계속 쓸 토큰(새로 발급됐거나, 기존 토큰을 그대로 에코)" }
```

### PC → 폰: `hello-deny` (거부 또는 타임아웃 — 이후 연결을 끊는다)

```json
{ "type": "hello-deny", "reason": "denied" }
```

`reason`은 `"denied"`(사용자가 거부) 또는 `"timeout"`(60초 안에 응답 없음).

### PC → 폰: `layout` (연결 시 + 배치 변경 시마다 전체 재전송)

```json
{
  "type": "layout",
  "pc": "WORK",
  "volume": 0.65,
  "buttons": [
    { "id": "s0", "label": "Chrome", "icon": "data:image/png;base64,..." },
    { "id": "s1" },
    { "id": "s2", "label": "메모장", "icon": "data:image/png;base64,..." }
  ]
}
```

`buttons`는 항상 16개 원소를 순서대로 담는다(`s0`부터 `s15`까지). `volume`은 PC의 현재 시스템 볼륨(0~1) — 매 `layout` 전송마다 새로 읽어서 넣는다.

### 폰 → PC: `press`

```json
{ "type": "press", "id": "s0" }
```

PC는 `id`로 내부 대상을 찾아 실행한다(exe는 직접 spawn, 폴더/문서/셸 특수위치는 탐색기로 열기). 매핑 없는 id(빈 슬롯)는 무시.

### 폰 → PC: `volume`

```json
{ "type": "volume", "level": 0.42 }
```

PC 시스템 볼륨을 즉시 그 값으로 설정하고, `volume-sync`를 모든 연결된 클라이언트에 재전송한다(PC 자신의 다이얼도 동기화됨).

### PC → 클라이언트: `volume-sync`

```json
{ "type": "volume-sync", "volume": 0.42 }
```

볼륨만 바뀌었을 때 보내는 가벼운 메시지 - 드래그 중엔 초당 여러 번 나갈 수 있어서, 매번 `layout`(아이콘 base64까지 포함해 수백 KB) 전체를 다시 보내면 느려진다. 버튼 배치는 그대로이므로 `buttons`는 안 담고 `volume`만 갱신한다.

## PC 내부 상태 (프로토콜 밖, 참고용)

- 버튼 배치는 PC 앱 데이터 폴더에 `buttons.json`으로 영속화 (`{ id, target, kind, label, icon }[]`). `target`은 exe·폴더·파일 경로 또는 `shell:` 특수 위치, `kind`가 `exe`(직접 실행)인지 `open`(탐색기로 열기)인지 결정한다. 예전 파일은 `exe_path` 키였는데 계속 읽히도록 alias 처리돼 있다.
- 아이콘은 대상에서 1회 추출해 base64 PNG로 캐시, 재구동 시 재추출하지 않음.
- 페어링된 기기는 `paired_devices.json`에 `{ device_id, device_name, token, paired_at }[]`로 영속화.

## 페이지는 프로토콜 밖 (클라이언트 로컬 개념)

- **페이지1(아이콘)**만 이 프로토콜로 오간다.
- **페이지2(설정)**는 각 앱이 자기 기기 정보만으로 그리는 화면이라 와이어 프로토콜에 없다. 예: 폰의 와이파이 SSID, mDNS 재검색 버튼 — 전부 폰 로컬 상태.

## 다음 확장 예정 (지금은 구현 안 함)

- 그리드 칸 수 자체가 가변(16 고정 → 사용자 설정 가능)이 되거나, 페이지 다중화(아이콘 페이지가 여러 장).
- 폰에서 편집 요청을 PC로 보내는 `edit` 메시지 (지금은 PC 전용 편집).
